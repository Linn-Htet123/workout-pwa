# Copy this file to ".env.local" and paste your real key for local testing.
# On Vercel, add the SAME variable under: Project → Settings → Environment Variables.
# NEVER commit your real key. ".env.local" is git-ignored on purpose.

# Get a key at https://console.anthropic.com  (Settings → API Keys)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
